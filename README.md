# Edwiin Portfolio

A static, immersive UI/UX design portfolio prepared for GitHub Pages.

## Local preview

```bash
python3 -m http.server 4173
```

Open `http://localhost:4173`.

## Publish

Create a public GitHub repository named `edwiin.github.io`, push these files to its default branch, then enable GitHub Pages from the repository root.
